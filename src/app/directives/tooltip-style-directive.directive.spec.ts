import { TooltipStyleDirectiveDirective } from './tooltip-style-directive.directive';

describe('TooltipStyleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipStyleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
